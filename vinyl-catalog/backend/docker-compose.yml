services:
  vinyl-vault-api:
    image: git.gsaraiva.com.br/gsaraiva2109/vinyl-vault-api:latest
    pull_policy: always
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: "3001"
      DATABASE_URL: postgres://vinyl:${POSTGRES_PASSWORD}@postgres:5432/vinyl
      AUTH_ENABLED: "true"
      AUTHENTIK_JWKS_URL: ${AUTHENTIK_JWKS_URL}
      AUTHENTIK_ISSUER: ${AUTHENTIK_ISSUER}
      DISCOGS_TOKEN: ${DISCOGS_TOKEN}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS}
    networks:
      - internal
      - dokploy-network

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: vinyl
      POSTGRES_USER: vinyl
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vinyl -d vinyl"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - internal

networks:
  internal:
    driver: bridge
  dokploy-network:
    external: true

volumes:
  pgdata:
