services:
  vinyl-catalog-web:
    image: ghcr.io/gsaraiva2109/vinyl-catalog-web:latest
    pull_policy: always
    restart: unless-stopped
    environment:
      HOSTNAME: "0.0.0.0"
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
